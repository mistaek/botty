//Could make this a class with a constructor for convenience but too lazy
export type Song = {
    url: string;
    title: string;
    thumbnail: string;
    duration: string; // do i want this as number??
}